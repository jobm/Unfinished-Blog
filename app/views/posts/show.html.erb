<div class="container">
	
	<p id="notice"><%= notice %></p>

	<h2>
  	<%= @post.title %>
	</h2>

	<p>
  	<%= @post.body %>
	</p>
	<div class="btn-group">
		<p class="btn btn-info" ><%= link_to 'Edit', edit_post_path(@post)%></p>
		<p class="btn btn-primary" ><%= link_to 'Back', posts_path%></p>
	</div>
</div>
